Example for configuration files

# Limit size in Kb that the code can have
CODE_SIZE_LIMIT

# Limit size that the output can have
OUTPUT_SIZE_LIMIT

# Time for the program check out some job in the queue
SLEEP_TIME

# Global option to receive the base directory 
BASE_DIR

# Limit size of default memory
DEFAULT_MEMORY_LIMIT

# Global option set to 1, that show the step by step of the runnables
#file printed to the output environment
DEBUG

# Global option that set to 1 as default, which sets the color
# pattern for the terminal. 1 for dark terminals, 2 for white
# and 0 for all echos to be standard colors
COLOR_SCHEME

# The interval between each test to check if the program has finished running
STEP_TIME
